server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # 纯静态单页应用：所有资源都为构建产物，history 路径回退到 index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
}
